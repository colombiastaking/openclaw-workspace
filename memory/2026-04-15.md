# 2026-04-15

## Website Upload - cPanel FTP Issue

Sebas asked why the website wasn't online. Website files live at `~/.openclaw/workspace/Website/` (eng, esp, fr). Website was last pushed via cPanel-update skill.

**FTP paths:**
- EN: `/public_html/` (lives directly at domain root)
- ES: `/esp.colombia-staking.com/`
- FR: `/fr.colombia-staking.com/`

Credentials in `~/.openclaw/secrets/.ftp_credentials`. FTP server accepts implicit TLS on port 990.

**Note:** The cpanel-update skill needs review — I was uploading to wrong paths (MKD into wrong directories). Verified one test file uploads correctly to `/public_html/` via curl with `--ftp-ssl`.

## Network Explorer - Sidebar Collapse Fix Pushed

Fixed sidebar collapse button disappearing when sidebar is hidden. Added a floating expand button (▶) that appears on the left edge when sidebar is collapsed on desktop.

**Push:** `git -C network-explorer add -A && git commit -m "sidebar: add expand button for desktop collapse" && git push`

## Pending: Website Upload to cPanel

Not yet done — was in progress when this flush happened. Need to run the upload script and verify site is live at colombia-staking.co