# Memory - 2026-04-07

## Ledger BTC xpub Investigation (Ongoing)

**Problem:** Trying to find BTC balance on a Ledger Bitcoin xpub. Sebas says there IS a balance but blockchain.info shows 0 across 2000 addresses.

**Key finding:** Sebas mentioned address `bc1qa90vtxkj0umyfzs5m2cr3n4c8f5pxnenfah4e0` — this is a **bc1q** (native SegWit) address starting with `bc1qa9...`. This format IS derived by BIP84 (our xpub), but:
- I scanned indices 0-30 on external chain → all empty
- I scanned indices 0-30 on internal chain (change) → all empty
- blockchain.info returns "not-found" for this address

**Status:** Sebas confirmed the address "must be included" — meaning it should be derivable from the xpub. The xpub we're using might be wrong, or it's a different derivation path. Investigation ongoing.

**Next steps:** Need to verify the correct xpub with Sebas. The bc1qa90... address exists on-chain but wasn't found in our derivation scan.

---


## Ledger BTC xpub Investigation — Update 16:23 UTC

**Scan results:** 2000 addresses scanned on external + internal BIP84 chains via blockchain.info.
- All 80 batched queries returned `"not-found-or-invalid-arg"` — blockchain.info confirmed no address has ever received a transaction
- The address `bc1qa90vtxkj0umyfzs5m2cr3n4c8f5pxnenfah4e0` does appear on-chain (5,844,519 sats received, unspent) but is NOT derivable from the xpub shared

**Key discrepancy:** blockchain.info shows the bc1qa90... address has received funds, but our xpub derivation finds nothing. This means:
- Either the xpub shared is incorrect/wrong account
- Or it's a different derivation path (Legacy BIP44, SegWit-P2SH BIP49)

**Action needed:** Sebas to provide the correct xpub from Ledger Live (`Settings → Bitcoin → Advanced → Show public key`).

**Stopped:** Scan abandoned per Sebas's request.

---


## Ledger BTC xpub — Key Insight (16:28 UTC)

**Blockchain.com mystery solved:**
- blockchain.com API returns 0 BTC (same as blockchain.info)
- But blockchain.com WEBSITE shows 0.05853943 BTC with 2 transactions
- Reason: website uses the **private key** (accessed via Ledger browser extension/wallet connection) for **hardened derivation**
- Our xpub only allows non-hardened derivation, generating a completely different address set

**Conclusion:** BTC IS on the Ledger, accessible via hardened BIP84 derivation which requires the private key. This is NOT lost — blockchain.com just has private key access.

**Options to recover:**
1. Export individual addresses from Ledger Live that have received BTC
2. Reverse-engineer blockchain.com's derivation path (requires knowing approx how many addresses used)
3. Use the Ledger itself directly with a tool that can do full BIP84 derivation

**Stopped per Sebas request.** No active scanning.

---


## Ledger BTC xpub — Sebas Clarification (16:39 UTC)

**Correction:** Sebas entered the xpub directly on blockchain.com — the Ledger was NEVER connected. So my "hardened derivation via private key" theory was wrong.

**What this means:**
- blockchain.com's xpub-only derivation SHOULD match what I compute
- But blockchain.com website shows 0.058 BTC while our derivation shows 0
- Either: blockchain.com is computing a DIFFERENT derivation path from xpub, OR the xpub shared doesn't match the bc1qa90... address

**Key fact:** The bc1qa90... address IS on-chain (received 5,844,519 sats, unspent). This address should be derivable from the correct xpub.

**Next step:** Need the correct xpub. Sebas to export from Ledger Live: `Settings → Bitcoin → Advanced → Show public key`.

**Investigation paused until Sebas provides correct xpub.**

---
