# 2026-04-12 — Daily Memory

## Network Explorer — Big Day

### Edge-Building Overhaul (STEP A/B/C)

**Morning session** (04:00-06:00):
- Fixed `normKey` hyphen stripping bug (fellowship-07 ≠ fellowship-staking)
- Removed `hasApiIdentity` guard from `findObserverIPsForGroup`
- Cross-shard IP acceptance (all shards, not per-shard)
- Added keyword + vowel-stripped matching for STEP C name extraction
- **Result:** GREEN edges went from 0 → 68, then 267, then 405

**Providers fixed via STEP C keywords:**
vapor (54/54), partnerstaking (114/114), eapes (54/54), ms (48/48), fellowship (20/20), moonlorian (26/26), wavenode (36/36), meria (152/152 ✅ added), bober (112/112 ✅ added), mregld (104/104 ✅ added)

**Still uncovered:** ofero (0 green), media (0 matched)

### Blue Edge "Mess" Investigation

- Sebas thought different providers at same IP was a bug
- **Confirmed:** Blue edges are working as designed — aggregate ALL IPs from ALL observers of identity across ALL shards
- Many providers legitimately co-host at same datacenter IPs (OVH, Hetzner, DigitalOcean)
- No bug — explained to Sebas

### Mobile UI Fixes

**Problem:** After mobile drawer CSS, filters invisible on mobile
**Fixes applied:**
- `display: flex` on `.sidebar` mobile (818095a)
- Compact header + stats values-only (eccc577)
- Bottom drawer (slides up from bottom, 55vh)
- ⚙ toggle button (bottom-right)
- Overlay behind drawer (tap outside to close)
- ✕ Close button in drawer
- Auto-close on filter chip tap

**NOT applied (kept sigma behavior stable):**
- `_swapGraph` optimization removed (kept ForceAtlas2 restart on filter change)
- Camera zoom limits
- `touch-action: none`
- `user-scalable=no`

**Stable commit:** `493a3de` — pushed to master

### Critical DB Bug Found

`node_observations` table has **NO `identity` column** — schema is `ts, pid, bls, name, shard, type, source`. Any query on `node_observations.identity` will fail with `no such column: identity`. Identity must come from `api_nodes` or `pidToInfo` enrichment.

### Skill Updated

SKILL.md at `~/.openclaw/workspace/.agents/skills/multiversx-network-map/SKILL.md` updated with full STEP C system, four edge types, mobile UI section, known issues.

## Other

- BTC strategy ran normally (no score change needed)
- No other major tasks
# 2026-04-12 — Daily Memory

## Network Explorer — Big Day

### Edge-Building Overhaul (STEP A/B/C)

**Morning session** (04:00-06:00):
- Fixed `normKey` hyphen stripping bug (fellowship-07 ≠ fellowship-staking)
- Removed `hasApiIdentity` guard from `findObserverIPsForGroup`
- Cross-shard IP acceptance (all shards, not per-shard)
- Added keyword + vowel-stripped matching for STEP C name extraction
- **Result:** GREEN edges went from 0 → 68, then 267, then 405

**Providers fixed via STEP C keywords:**
vapor (54/54), partnerstaking (114/114), eapes (54/54), ms (48/48), fellowship (20/20), moonlorian (26/26), wavenode (36/36), meria (152/152 ✅ added), bober (112/112 ✅ added), mregld (104/104 ✅ added)

**Still uncovered:** ofero (0 green), media (0 matched)

### Blue Edge "Mess" Investigation

- Sebas thought different providers at same IP was a bug
- **Confirmed:** Blue edges are working as designed — aggregate ALL IPs from ALL observers of identity across ALL shards
- Many providers legitimately co-host at same datacenter IPs (OVH, Hetzner, DigitalOcean)
- No bug — explained to Sebas

### Mobile UI Fixes

**Problem:** After mobile drawer CSS, filters invisible on mobile
**Fixes applied:**
- `display: flex` on `.sidebar` mobile (818095a)
- Compact header + stats values-only (eccc577)
- Bottom drawer (slides up from bottom, 55vh)
- ⚙ toggle button (bottom-right)
- Overlay behind drawer (tap outside to close)
- ✕ Close button in drawer
- Auto-close on filter chip tap

**NOT applied (kept sigma behavior stable):**
- `_swapGraph` optimization removed (kept ForceAtlas2 restart on filter change)
- Camera zoom limits
- `touch-action: none`
- `user-scalable=no`

**Stable commit:** `493a3de` — pushed to master

### Critical DB Bug Found

`node_observations` table has **NO `identity` column** — schema is `ts, pid, bls, name, shard, type, source`. Any query on `node_observations.identity` will fail with `no such column: identity`. Identity must come from `api_nodes` or `pidToInfo` enrichment.

### Skill Updated

SKILL.md at `~/.openclaw/workspace/.agents/skills/multiversx-network-map/SKILL.md` updated with full STEP C system, four edge types, mobile UI section, known issues.

## Other

- BTC strategy ran normally (no score change needed)
- No other major tasks

---

## Pre-Compaction Flush — 2026-04-12 06:45 Bogota

All durable memories already stored above (morning session) and in SKILL.md. Nothing new to store.


---

## Network Explorer — STEP C Keyword Refinement (Afternoon — 2026-04-12 20:08 UTC)

### Sebas asked: Find validators with zero edges and match to observer pseudo names

**Goal:** Find provider identities with 0 edges (no red/blue/green) and see if their validator name prefix matches any observer pseudo name → if yes, add as new STEP C keyword.

**Method:** Queried DB for zero-edge identities, then matched validator name prefix → observer pseudo. Found 12 clean matches:

### New keywords to add (pending Sebas approval):
```
truststaking, xoxno, oxsym, mapleleaf, stakegennode,
projectx, cavaler, egldsqueeze, disruptive, buidly,
iversevision10b, moldoveanu_
```

### Keywords to skip (false positives / clashes):
- `private` — too generic (multiple identities use `Private-##`)
- `starfield` — would clash with `star_staking` partial
- `vbsmains` — too close to `vbs-main-s0-20` validator pattern
- `metachain wavenode` — too specific, clashes with existing `wavenode`
- `castlestakecommetachainm` — too long, normalization lossy

### Result sent to Sebas — awaiting approval to implement.

---
## Network Explorer Collector Run (20:01 UTC)

Collector completed successfully in 179.1s:
- 5343 nodes, 4710 edges saved (9.2MB)
- 8 observers processed
- 55 Colombia nodes (Pi: 186.99.3.198)
- DB: 166,531 peer observations, 154 runs
- Gateway heartbeatstatus failed (ENOTFOUND gateway.mvxscan.com)
