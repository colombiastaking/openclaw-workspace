# Daily Notes - 2026-04-05

## DApp LiteSpeed Cache Issue (Ongoing)

**Problem:** LiteSpeed on `staking.colombia-staking.com` serves HTML for ALL file types (`.js`, `.css`, etc.) - severe cache corruption. Even `index2.html` got 404 after purge attempt.

**Root Cause:** LiteSpeed cache is broken/misconfigured on the cPanel hosting.

**Fix Applied:**
- Updated `src/staking.ts` with working API URLs:
  - `gateway.multiversx.com` for SC queries (was `staking.colombia-staking.com/mvxproxy.php/gateway/query`)
  - `colombia-staking.co/api` for REST API (via cloudflared → kepler-proxy)
- `index-bee006ae.js` is the correct new build (confirmed working via Pi direct access)

**Permanent Solution (In Progress):**
- DApp server now running on Pi port 4080: `python3 -m http.server 4080` in `workspace/DAPP-V3/build/`
- Service: `dapp-server.service` (systemd user service)
- Need cloudflared route for `staking.colombia-staking.co` → port 4080

**DNS Record Needed (Waiting on Sebas):**
- Type: CNAME, Name: `staking`, Target: `colombia-staking.co`, Proxy: Proxied
- This will make DApp accessible via cloudflared tunnel, bypassing LiteSpeed

## Kepler Proxy Updates

- Added `/gateway/*` route → `https://kepler-api.projectx.mx/mainnet/gateway/` (was missing)
- SC queries now route to `gateway.multiversx.com` which works correctly
- `/api/*` route works for REST API calls

## GitHub
- Fix committed: `1cccf16` - DApp API URL fixes

## Bitcoin Strategy
- Weekly report generated earlier today (around 8:30 AM GMT-5)
