# Daily Notes - 2026-04-20

## Website Updates (Colombia Staking)

### Network Explorer (colombia-staking.co)
- Removed "About Colombia Staking" dropdown nav from homepage
- Fixed missing `</nav>` tag that hid page content
- Nav is now clean — logo only, no links
- Deployed via FTP with `deploy-website.sh`

### Website Homepage
- Added `stats-updater.js` + live node count to EN/ES/FR `index.html`
- Added `fetchLiveStats()` call to EN/ES/FR `index.html`
- Deployed all 3 sites (eng/esp/fr) — 41446 / 42130 / 42325 bytes

### About Page (quienes-somos.html) — LIVE STATS
- Added `stat-label` class to all 4 stats (was `label`)
- Updated ES/FR labels to match `LABEL_MAP` keys:
  - EN: "Nodes", "Uptime", "Green Energy", "Monitoring"
  - ES: "Nodos", "Uptime", "Energía Verde", "Monitoreo 24/7"
  - FR: "Nœuds", "Disponibilité", "Énergie Verte", "Surveillance 24/7"
- Added `stats-updater.js` include to all 3 language versions
- **LiteSpeed cache issue**: Files uploaded but still serving old cached version (Mar 15). Manual cPanel purge needed.

### Deploy Script Fixed
- `~/.openclaw/workspace/scripts/deploy-website.sh` — retry logic for transient FTP errors (exit 9/28)
- 1-second delay between files to avoid FTP rate limiting
- Uses `colombia6` FTP account

## Kepler Proxy — kepler-api.projectx.mx DOWN
- `stats-api.php` ✅ works (local fallback)
- `/gateway/*` ❌ 502 Bad Gateway — kepler-api.projectx.mx is offline
- `/api/*` ❌ 502 — same cause
- kepler-proxy.js service on Pi is fine; upstream target is down
- Waiting on Kepler team for resolution

## COLS Distribution — Redundant Cron Removed
- `daily-distribution-summary` (2:00 PM cron) was duplicating the 1:10 PM report
- Removed the 2:00 PM summary cron job
- BONUS distribution: 24.01 COLS → 104 recipients ✅
- GOLD (21 recipients, 4.28 COLS) and DAO still pending from today

## cPanel LiteSpeed Cache — Still Unresolved
- Cannot auto-purge via API token — LiteSpeed API module not installed
- Manual purge in cPanel UI needed (LiteSpeed Cache → Purge All)
- After one manual purge, deploy script should keep files fresh (LiteSpeed respects timestamps)

## GitHub Commits Today
- `685144e` — live stats on website
- `0abb7ac` — distribution fix (cascading failure)
- `e27ccad` — FTP memory update
- `b2cedfa` — skill update
