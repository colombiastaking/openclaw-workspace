# 2026-04-06 - DApp V3 Gateway Fixes

## DApp V3 Critical Fixes

### Problem
DApp was returning "API error" on SC queries. Root cause: double-slash 404.
- `PRIMARY_GATEWAY` had trailing slash: `https://colombia-staking.co/gateway/`
- SDK appended paths directly → `gateway//network/config` → 404

### Fixes Applied

1. **`0fe5be6`** — Removed trailing slash from `PRIMARY_GATEWAY`
   - File: `src/config.ts`
   - Changed: `gateway/` → `gateway`

2. **`b59383e`** — New `FallbackProxyNetworkProvider`
   - File: `src/helpers/FallbackProxyNetworkProvider.ts`
   - Wraps all SC queries: tries Colombia Kepler → falls back to public `gateway.multiversx.com`
   - Applied to: `useGlobalData.ts`, `usePreloadData.ts`, `useColsApr.ts`
   - Health check at startup resets failed endpoints if primary recovers

3. **`ebc969b`** — COLS APR MEX price fix
   - Uses `api.multiversx.com` MEX price as primary instead of direct gateway

4. **`1be0114`** — Colombia gateway instead of public
   - All SC queries routed through `colombia-staking.co/gateway`

5. **`1cccf16`** — Replaced broken `mvxproxy.php` with Kepler proxy

### Deployed
- `index-cd871205.js` live on cPanel ✅
- All source committed and pushed to GitHub ✅
- GitHub at `b59383e` (in sync with local)

### Files Changed
- `src/config.ts` — removed trailing slash
- `src/helpers/FallbackProxyNetworkProvider.ts` — NEW
- `src/hooks/useGlobalData.ts` — uses FallbackProvider
- `src/hooks/usePreloadData.ts` — uses FallbackProvider
- `src/hooks/useColsApr.ts` — uses FallbackProvider

### Verified Working
- Both `https://colombia-staking.co/gateway/network/config` ✅
- `https://gateway.multiversx.com/network/config` ✅
- DApp SC queries resolving correctly ✅
