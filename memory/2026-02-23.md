# Session: 2026-02-23 17:45 UTC

## Work Done Today

### MultiversX Local Gateway (Proxy) Setup

Successfully installed and configured mx-chain-proxy-go on 192.168.0.120:

1. **Installed Proxy** on port 8079
2. **Key insight**: Each observer is on separate machine - must connect via nginx (port 8008), not directly to observer (8080)
3. **Final working config**: Proxy → nginx (8008) → observer nodes (shards 0,1,2,meta)
4. **Elasticsearch**: Configured external.toml to connect to 192.168.0.120:9200

### Distribution Scripts - Local First

Updated scripts to prioritize local infrastructure:

1. **calculate_distribution_v4.mjs**: Changed PRIMARY_GATEWAY from `http://192.168.0.120/gateway` to `http://192.168.0.120:8079`

2. **calculate_gold_distribution.mjs**: Rewrote to use local API first:
   - Gold NFT owners: local first, external fallback
   - Prices: local first, external fallback
   - APR: local first, external fallback
   - Delegations: local first, external fallback

### Verified Working

- Port 3001 (API Service): Full functionality
- Port 8079 (Proxy): VM queries work
- Local infrastructure for daily distributions is operational

---

### Public Tunnel (ngrok)

Set up ngrok to expose local API publicly:

1. **Installed ngrok** via apt: `sudo apt install ngrok`
2. **Authtoken configured**: 3A5CrTQzS9rCKfcEDSx79Yk9mXO_4vhNBJZdo84v7yEYf8gAN
3. **Started tunnel**: `ngrok http 8080` (port 8080 → nginx → local API)
4. **Public URL**: `https://belle-nonpossessed-valda.ngrok-free.dev`

### DApp Updated with Local API

Updated DApp to use ngrok URL as primary API:

1. **Config updated** with 3-level fallback:
   - Primary: ngrok (belle-nonpossessed-valda.ngrok-free.dev)
   - Secondary: staking.colombia-staking.com/mvx-api
   - Tertiary: api.multiversx.com

2. **Gateway fallback chain**:
   - Primary: ngrok gateway (/gateway)
   - Secondary: staking.colombia-staking.com/gateway
   - Tertiary: gateway.multiversx.com

3. **Fixed Gold Member NFT check** - was using wrong endpoint

4. **Deployed**: Commit hash dceaa571 with new API URLs

### Current URLs

| Service | Local | Public (ngrok) |
|---------|-------|----------------|
| API | 192.168.0.120:3001 | https://belle-nonpossessed-valda.ngrok-free.dev |
| Gateway | 192.168.0.120:8079 | https://belle-nonpossessed-valda.ngrok-free.dev/gateway |

### Notes

- ngrok free tier shows warning page in browser (expected)
- For clean browser URL, would need paid ngrok or Cloudflare Tunnel
- Local API is missing price data (comes from MEX indexer)

### Documentation Updated

- Skill: `~/.openclaw/workspace/skills/mx-proxy/SKILL.md`
- TOOLS.md: Added complete "Proxy Installation" section

## Key Notes

- Always connect proxy to nginx (8008), not directly to observer (8080)
- Local API = port 3001, Proxy = port 8079
- Distribution scripts now use local first, external as backup
