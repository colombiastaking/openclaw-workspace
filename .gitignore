# Secrets and credentials — NEVER commit these
monitoring.env
.secrets/
.env
*.enc.key
wallet_raw*
*.private_key
.private_key
wallet/
telegram.env
*.env

# API keys and tokens
*.api.key
*.token
apikey*
kepler*.key

# Cloudflare / tunnel credentials
cloudflared/
tunnel*.json

# Passwords and recovery
*password*
*recovery*
*.bak
.local_secrets.json

# OpenClaw workspace state (may include runtime metadata)
openclaw-workspace-state.json

# Node / Python build artifacts
node_modules/
dist/
build/
*.pyc
__pycache__/

# Memory recall indexes (can contain sensitive snippets)
memory/.dreams/

# IDE / OS
.DS_Store
.vscode/
.idea/
